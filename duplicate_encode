def duplicate_encode1(word):
    word = word.lower()
    new_str = ''
    for letter in word:
        if word.count(letter) == 1:
            new_str = new_str + "("
        else:
            new_str = new_str + ")"
    return new_str
